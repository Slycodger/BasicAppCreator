public unsafe class BufferType {
  //(IntPtr)buffer
  //--------------------------------------------------
  public IntPtr buffer;
  //--------------------------------------------------





  public BufferType(IntPtr ptr) {
    buffer = ptr;
  }
}